import { Routes } from '@angular/router';
import { GroupList } from './component/group-list/group-list';

export const routes: Routes = [
    { path: '', component: GroupList }, // <- landing
    // other routes
];
